-- aydo hub v2.0 PREMIUM - %100 DOĞRULANMIŞ FİNAL SÜRÜM 
-- (SAHTE YÜKLEME + BİLDİRİM SİSTEMİ + SES EFEKTLERİ + ANİMASYONLU UI)
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")

local localPlayer = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- DURUM DEĞİŞKENLERİ
local espEnabled = false
local isEnabled = false
local connection = nil
local monsterEspEnabled = false
local rKeyEnabled = false
local flyEnabled = false
local ragdollEnabled = false
local noclipEnabled = false
-- AIM ASSIST & FOV AYARLARI
local fovRadius = 120 -- Halkanın boyutu (bu sayıyı degistirebilirsni)
local isAimLocked = false
local aimTarget = nil

-- Ekran üzerine FOV Halkası cizimi
local fovCircle = Drawing.new("Circle")
fovCircle.Thickness = 2
fovCircle.NumSides = 64
fovCircle.Radius = fovRadius
fovCircle.Filled = false
fovCircle.Visible = false
fovCircle.Color = Color3.fromRGB(255, 182, 193) -- Halka Rengi (pempe)

local isMinimized = false
local isAttached = false
local isFlying = false
local autoBlockEnabled = false
local VirtualInputManager = game:GetService("VirtualInputManager")
local isFleeing = false
local originalPos = nil
local isHoldingF = false
-- HIZ & ZIPLAMA DEĞİŞKENLERİ
local speedLoopEnabled = false
local jumpLoopEnabled = false
local aimAssistEnabled = false
local selectedSpeed = 16
local selectedJump = 50
local isYonelmeActive = false
local yonelmeConn = nil
local currentAlignOri = nil
local currentAtt = nil

-- UÇMA AYARLARI
local flySpeed = 350
local flyBodyVelocity, flyBodyGyro

---------------------------------------------------------
-- SES EFEKTİ SİSTEMİ (YENİ)
---------------------------------------------------------
local function PlaySound(soundType)
    local sound = Instance.new("Sound")
    sound.Parent = SoundService
    
    if soundType == "Hover" then
        sound.SoundId = "rbxassetid://6895079853"
        sound.Volume = 0.5
    elseif soundType == "Click" then
        sound.SoundId = "rbxassetid://6655851046"
        sound.Volume = 0.8
    elseif soundType == "Success" then
        sound.SoundId = "rbxassetid://2865227271"
        sound.Volume = 1
    elseif soundType == "Error" then
        sound.SoundId = "rbxassetid://6655851046"
        sound.Volume = 0
    elseif soundType == "Tick" then
        sound.SoundId = "rbxassetid://6655851046"
        sound.Volume = 0
    end
    
    sound:Play()
    game.Debris:AddItem(sound, 2)
end

---------------------------------------------------------
-- OPTİMİZE HEDEF BULUCU (FPS DOSTU) - (MANTIK KORUNDU)
---------------------------------------------------------
local function checkTarget(obj, myPos, currentMinDist, currentClosest)
    if obj:IsA("Model") and obj ~= localPlayer.Character then
        local hum = obj:FindFirstChildOfClass("Humanoid")
        local hrp = obj:FindFirstChild("HumanoidRootPart")
        if hum and hrp and hum.Health > 0 then
            local dist = (hrp.Position - myPos).Magnitude
            if dist < currentMinDist then return dist, hrp end
        end
    end
    return currentMinDist, currentClosest
end

local function getNearestTarget()
    local myChar = localPlayer.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return nil end
    
    local myPos = myChar.HumanoidRootPart.Position
    local closestHRP = nil
    local minDist = math.huge
    
    -- Hedeflerin bulunduğu klasörü referans alıyoruz
    local liveFolder = workspace:FindFirstChild("Live")
    
    if liveFolder then
        for _, target in pairs(liveFolder:GetChildren()) do
            -- Kendimizi hedef almamak için kontrol (Eğer karakterimiz o klasördeyse)
            if target ~= myChar then
                -- checkTarget fonksiyonunu kullanarak mesafeyi ve en yakın hedefi güncelle
                minDist, closestHRP = checkTarget(target, myPos, minDist, closestHRP)
            end
        end
    else
        warn("Workspace.Live klasörü bulunamadı!")
    end

    return closestHRP
end

---------------------------------------------------------
-- UI OLUŞTURMA (PREMIUM & ANİMASYONLU)
---------------------------------------------------------
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AydoHubV2_Premium"
screenGui.ResetOnSpawn = false
screenGui.Parent = localPlayer:WaitForChild("PlayerGui")
screenGui.DisplayOrder = 999999
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

---------------------------------------------------------
-- BİLDİRİM SİSTEMİ (YENİ)
---------------------------------------------------------
local notifContainer = Instance.new("Frame", screenGui)
notifContainer.Size = UDim2.new(0, 250, 1, -50)
notifContainer.Position = UDim2.new(1, -270, 0, 25)
notifContainer.BackgroundTransparency = 1
local notifLayout = Instance.new("UIListLayout", notifContainer)
notifLayout.SortOrder = Enum.SortOrder.LayoutOrder
notifLayout.VerticalAlignment = Enum.VerticalAlignment.Bottom
notifLayout.Padding = UDim.new(0, 10)

local function SendNotification(titleText, descText, duration)
    duration = duration or 3
    PlaySound("Tick")
    
    local notifFrame = Instance.new("Frame", notifContainer)
    notifFrame.Size = UDim2.new(1, 0, 0, 60)
    notifFrame.BackgroundTransparency = 0.1
    notifFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    notifFrame.BorderSizePixel = 0
    notifFrame.Position = UDim2.new(1, 50, 0, 0) -- Başlangıçta sağda saklı
    Instance.new("UICorner", notifFrame).CornerRadius = UDim.new(0, 8)
    
    local stroke = Instance.new("UIStroke", notifFrame)
    stroke.Color = Color3.fromRGB(0, 170, 255)
    stroke.Thickness = 1.5
    
    local title = Instance.new("TextLabel", notifFrame)
    title.Size = UDim2.new(1, -15, 0, 25)
    title.Position = UDim2.new(0, 15, 0, 5)
    title.BackgroundTransparency = 1
    title.Text = titleText
    title.TextColor3 = Color3.fromRGB(0, 170, 255)
    title.Font = Enum.Font.GothamBold
    title.TextSize = 14
    title.TextXAlignment = Enum.TextXAlignment.Left
    
    local desc = Instance.new("TextLabel", notifFrame)
    desc.Size = UDim2.new(1, -15, 0, 25)
    desc.Position = UDim2.new(0, 15, 0, 30)
    desc.BackgroundTransparency = 1
    desc.Text = descText
    desc.TextColor3 = Color3.fromRGB(200, 200, 200)
    desc.Font = Enum.Font.GothamSemibold
    desc.TextSize = 12
    desc.TextXAlignment = Enum.TextXAlignment.Left
    desc.TextWrapped = true
    
    -- Giriş Animasyonu
    TweenService:Create(notifFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = UDim2.new(0, 0, 0, 0)}):Play()
    
    task.delay(duration, function()
        -- Çıkış Animasyonu
        TweenService:Create(notifFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Position = UDim2.new(1, 50, 0, 0)}):Play()
        task.wait(0.4)
        notifFrame:Destroy()
    end)
end

---------------------------------------------------------
-- ŞİFRE EKRANI
---------------------------------------------------------
local loginFrame = Instance.new("Frame", screenGui)
loginFrame.Size = UDim2.new(0, 300, 0, 160)
loginFrame.Position = UDim2.new(0.5, -150, 0.5, -80)
loginFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
loginFrame.BorderSizePixel = 0
loginFrame.Visible = false -- Yükleme ekranı bitene kadar gizli
Instance.new("UICorner", loginFrame).CornerRadius = UDim.new(0, 10)

local loginStroke = Instance.new("UIStroke", loginFrame)
loginStroke.Color = Color3.fromRGB(0, 170, 255)
loginStroke.Thickness = 2
loginStroke.Transparency = 0.2

local loginTitle = Instance.new("TextLabel", loginFrame)
loginTitle.Size = UDim2.new(1, 0, 0, 45)
loginTitle.BackgroundTransparency = 1
loginTitle.Text = "Aydo Hub Giriş"
loginTitle.TextColor3 = Color3.fromRGB(0, 170, 255)
loginTitle.Font = Enum.Font.GothamBlack
loginTitle.TextSize = 20

local passBox = Instance.new("TextBox", loginFrame)
passBox.Text = ""
passBox.Size = UDim2.new(0.8, 0, 0, 40)
passBox.Position = UDim2.new(0.1, 0, 0, 50)
passBox.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
passBox.TextColor3 = Color3.fromRGB(255, 255, 255)
passBox.PlaceholderText = "Şifreyi Girin..."
passBox.Font = Enum.Font.GothamSemibold
passBox.TextSize = 14
passBox.ClearTextOnFocus = false
Instance.new("UICorner", passBox).CornerRadius = UDim.new(0, 6)
local passStroke = Instance.new("UIStroke", passBox)
passStroke.Color = Color3.fromRGB(60, 60, 70)

local loginBtn = Instance.new("TextButton", loginFrame)
loginBtn.Size = UDim2.new(0.6, 0, 0, 35)
loginBtn.Position = UDim2.new(0.2, 0, 0, 105)
loginBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
loginBtn.Text = "GİRİŞ YAP"
loginBtn.TextColor3 = Color3.new(1, 1, 1)
loginBtn.Font = Enum.Font.GothamBold
loginBtn.TextSize = 14
Instance.new("UICorner", loginBtn).CornerRadius = UDim.new(0, 6)
local loginScale = Instance.new("UIScale", loginFrame)
loginScale.Scale = 0

---------------------------------------------------------
-- SAHTE YÜKLEME EKRANI (YENİ - ÇAĞ ATLATAN GİRİŞ)
---------------------------------------------------------
local loadFrame = Instance.new("Frame", screenGui)
loadFrame.Size = UDim2.new(0, 320, 0, 100)
loadFrame.Position = UDim2.new(0.5, -160, 0.5, -50)
loadFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
Instance.new("UICorner", loadFrame).CornerRadius = UDim.new(0, 10)
local loadStroke = Instance.new("UIStroke", loadFrame)
loadStroke.Color = Color3.fromRGB(0, 170, 255)
loadStroke.Thickness = 2

local loadTitle = Instance.new("TextLabel", loadFrame)
loadTitle.Size = UDim2.new(1, 0, 0, 35)
loadTitle.Position = UDim2.new(0, 0, 0, 10)
loadTitle.BackgroundTransparency = 1
loadTitle.Text = "Aydo Hub Başlatılıyor..."
loadTitle.TextColor3 = Color3.fromRGB(0, 170, 255)
loadTitle.Font = Enum.Font.GothamBlack
loadTitle.TextSize = 18

local loadBarBg = Instance.new("Frame", loadFrame)
loadBarBg.Size = UDim2.new(0.8, 0, 0, 12)
loadBarBg.Position = UDim2.new(0.1, 0, 0, 55)
loadBarBg.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
Instance.new("UICorner", loadBarBg).CornerRadius = UDim.new(1, 0)

local loadBarFill = Instance.new("Frame", loadBarBg)
loadBarFill.Size = UDim2.new(0, 0, 1, 0)
loadBarFill.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
Instance.new("UICorner", loadBarFill).CornerRadius = UDim.new(1, 0)

local loadPct = Instance.new("TextLabel", loadFrame)
loadPct.Size = UDim2.new(1, 0, 0, 20)
loadPct.Position = UDim2.new(0, 0, 0, 75)
loadPct.BackgroundTransparency = 1
loadPct.Text = "%0"
loadPct.TextColor3 = Color3.fromRGB(150, 150, 150)
loadPct.Font = Enum.Font.GothamBold
loadPct.TextSize = 12

local loadScale = Instance.new("UIScale", loadFrame)
loadScale.Scale = 0

-- Yükleme Ekranı Mantığı (2 Saniye %10, 1 Saniye %100)
task.spawn(function()
    TweenService:Create(loadScale, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
    task.wait(0.5)
    
    -- %10'a çık (İlk 2 Saniye)
    TweenService:Create(loadBarFill, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {Size = UDim2.new(0.1, 0, 1, 0)}):Play()
    for i = 1, 10 do loadPct.Text = "%" .. i; task.wait(0.03) end
    task.wait(1.7) -- Kalan bekleme süresi (Toplam 2 saniye)
    
    -- %100'e fırla (Son 1 Saniye)
    TweenService:Create(loadBarFill, TweenInfo.new(1, Enum.EasingStyle.Quint, Enum.EasingDirection.InOut), {Size = UDim2.new(1, 0, 1, 0)}):Play()
    for i = 11, 100 do
        loadPct.Text = "%" .. i
        task.wait(1 / 90) -- Toplam 1 saniyede bitmesi için
    end
    
    loadPct.Text = "%100 - Hazır!"
    loadPct.TextColor3 = Color3.fromRGB(0, 255, 100)
    PlaySound("Success")
    task.wait(0.5)
    
    -- Yükleme ekranını kapat, şifre ekranını aç
    TweenService:Create(loadScale, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Scale = 0}):Play()
    task.wait(0.4)
    loadFrame:Destroy()
    
    loginFrame.Visible = true
    TweenService:Create(loginScale, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
end)

---------------------------------------------------------
-- ANA MENÜ (ŞİFRELİ GİRİŞE BAĞLANDI)
---------------------------------------------------------
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 270, 0, 450)
mainFrame.Position = UDim2.new(0.5, -135, 0.4, 0)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Visible = false
mainFrame.Parent = screenGui
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 10)

local mainScale = Instance.new("UIScale", mainFrame)
mainScale.Scale = 0

local mainStroke = Instance.new("UIStroke", mainFrame)
mainStroke.Color = Color3.fromRGB(0, 170, 255)
mainStroke.Thickness = 2
mainStroke.Transparency = 0.2

-- ŞİFRE DOĞRULAMA SİSTEMİ
local function attemptLogin()
    if passBox.Text == "1" then
        PlaySound("Success")
        passStroke.Color = Color3.fromRGB(0, 255, 0)
        loginBtn.Text = "BAŞARILI!"
        loginBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 50)
        
        SendNotification("Giriş Başarılı", "Aydo Hub Premium'a hoş geldiniz!", 4)
        task.wait(0.5)
        
        TweenService:Create(loginScale, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Scale = 0}):Play()
        task.wait(0.4)
        loginFrame:Destroy()
        
        mainFrame.Visible = true
        TweenService:Create(mainScale, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
    else
        PlaySound("Error")
        passStroke.Color = Color3.fromRGB(255, 0, 0)
        loginBtn.Text = "YANLIŞ ŞİFRE!"
        loginBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
        
        SendNotification("Giriş Başarısız", "Girdiğiniz şifre hatalı. Lütfen tekrar deneyin.", 3)
        
        local origPos = loginFrame.Position
        for i = 1, 6 do
            loginFrame.Position = origPos + UDim2.new(0, math.random(-8, 8), 0, math.random(-8, 8))
            task.wait(0.04)
        end
        loginFrame.Position = origPos
        
        task.wait(1)
        loginBtn.Text = "GİRİŞ YAP"
        loginBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
        passStroke.Color = Color3.fromRGB(60, 60, 70)
    end
end

loginBtn.MouseButton1Click:Connect(attemptLogin)
loginBtn.MouseEnter:Connect(function() PlaySound("Hover") end)

passBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then attemptLogin() end
end)

-- ÜST BAR & BAŞLIK
local topBar = Instance.new("Frame", mainFrame)
topBar.Size = UDim2.new(1, 0, 0, 45)
topBar.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
topBar.BorderSizePixel = 0
Instance.new("UICorner", topBar).CornerRadius = UDim.new(0, 10)

local topBarPatch = Instance.new("Frame", topBar)
topBarPatch.Size = UDim2.new(1, 0, 0, 10); topBarPatch.Position = UDim2.new(0, 0, 1, -10)
topBarPatch.BackgroundColor3 = Color3.fromRGB(25, 25, 30); topBarPatch.BorderSizePixel = 0

local titleLbl = Instance.new("TextLabel", topBar)
titleLbl.Size = UDim2.new(0, 100, 1, 0)
titleLbl.Position = UDim2.new(0, 12, 0, 0)
titleLbl.BackgroundTransparency = 1
titleLbl.Text = "Aydo Hub V2"
titleLbl.TextColor3 = Color3.fromRGB(0, 170, 255)
titleLbl.Font = Enum.Font.GothamBlack
titleLbl.TextSize = 15
titleLbl.TextXAlignment = Enum.TextXAlignment.Left
mainFrame.ZIndex = 10
topBar.ZIndex = 11

-- SEKMELER
local tabContainer = Instance.new("Frame", topBar)
tabContainer.Size = UDim2.new(1, -145, 1, 0); tabContainer.Position = UDim2.new(0, 110, 0, 0)
tabContainer.BackgroundTransparency = 1
Instance.new("UIListLayout", tabContainer).FillDirection = Enum.FillDirection.Horizontal

local function createTab(name)
    local b = Instance.new("TextButton", tabContainer)
    b.Size = UDim2.new(0, 60, 1, 0); b.BackgroundTransparency = 1; b.Text = name; 
    b.TextColor3 = Color3.fromRGB(200, 200, 200); b.Font = Enum.Font.GothamBold; b.TextSize = 12
    b.MouseEnter:Connect(function() PlaySound("Hover") end)
    b.MouseButton1Click:Connect(function() PlaySound("Click") end)
    return b
end

local tbTab = createTab("Ana"); local doorsTab = createTab("Doors")
local minBtn = Instance.new("TextButton", topBar)
local Rivalstab = createTab("Rİvals,")
minBtn.Size = UDim2.new(0, 30, 0, 30); minBtn.Position = UDim2.new(1, -35, 0, 7); 
minBtn.Text = "-"; minBtn.TextColor3 = Color3.new(1,1,1); minBtn.BackgroundTransparency = 1; minBtn.Font = Enum.Font.GothamBold; minBtn.TextSize = 20
minBtn.MouseEnter:Connect(function() PlaySound("Hover") end)

local isMinimized = false
minBtn.MouseButton1Click:Connect(function()
    PlaySound("Click")
    isMinimized = not isMinimized
    if isMinimized then
        mainFrame:TweenSize(UDim2.new(0, 270, 0, 45), "Out", "Quart", 0.3, true)
        tbPage.Visible = false
        doorsPage.Visible = false
    else
        mainFrame:TweenSize(UDim2.new(0, 270, 0, 450), "Out", "Quart", 0.3, true)
        task.wait(0.3)
        if tbTab.TextColor3 == Color3.fromRGB(0, 170, 255) then
            tbPage.Visible = true
        else
            doorsPage.Visible = true
        end
    end
end)

local aimAssistBtn = createBtn("AimAssist: KAPALI", tbPage)
local closeBtn = Instance.new("TextButton", topBar)
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -65, 0, 7)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 60, 60)
closeBtn.BackgroundTransparency = 1
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 20
closeBtn.MouseEnter:Connect(function() PlaySound("Hover") end)

closeBtn.MouseButton1Click:Connect(function()
    PlaySound("Click")
    SendNotification("Çıkış Yapılıyor", "Aydo Hub kapatıldı. Görüşmek üzere!", 2)
    task.wait(0.5)
    screenGui:Destroy()
end)

-- SAYFALAR
local function createPage()
    local sf = Instance.new("ScrollingFrame", mainFrame)
    sf.Size = UDim2.new(1, 0, 1, -45); sf.Position = UDim2.new(0, 0, 0, 45); sf.BackgroundTransparency = 1; sf.Visible = false
    sf.AutomaticCanvasSize = Enum.AutomaticSize.Y
    sf.ScrollBarThickness = 3
    sf.ScrollBarImageColor3 = Color3.fromRGB(0, 170, 255)
    local layout = Instance.new("UIListLayout", sf)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.Padding = UDim.new(0, 8)
    Instance.new("UIPadding", sf).PaddingTop = UDim.new(0, 10)
    Instance.new("UIPadding", sf).PaddingBottom = UDim.new(0, 10)
    return sf
end

tbPage = createPage()
tbPage.Visible = true

doorsPage = createPage()
RivalsPage = createPage()

local function applyAnimations(btn)
    local scale = Instance.new("UIScale", btn)
    btn.MouseEnter:Connect(function() 
        PlaySound("Hover")
        TweenService:Create(scale, TweenInfo.new(0.15), {Scale = 1.03}):Play() 
    end)
    btn.MouseLeave:Connect(function() TweenService:Create(scale, TweenInfo.new(0.15), {Scale = 1.0}):Play() end)
    btn.MouseButton1Down:Connect(function() 
        PlaySound("Click")
        TweenService:Create(scale, TweenInfo.new(0.1), {Scale = 0.95}):Play() 
    end)
    btn.MouseButton1Up:Connect(function() TweenService:Create(scale, TweenInfo.new(0.1), {Scale = 1.03}):Play() end)
end

local function createBtn(text, parent, bgColor, strokeColor)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(0.9, 0, 0, 35)
    btn.BackgroundColor3 = bgColor or Color3.fromRGB(35, 35, 40)
    btn.Text = text; btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.GothamSemibold; btn.TextSize = 13
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = strokeColor or Color3.fromRGB(60, 60, 70)
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    
    applyAnimations(btn)
    return btn
end

local function createSlider(text, parent, min, max, default, callback)
    local container = Instance.new("Frame", parent)
    container.Size = UDim2.new(0.9, 0, 0, 40); container.BackgroundTransparency = 1
    
    local label = Instance.new("TextLabel", container)
    label.Size = UDim2.new(1, 0, 0, 15); label.BackgroundTransparency = 1
    label.Text = text .. ": " .. default; label.TextColor3 = Color3.new(1,1,1)
    label.Font = Enum.Font.GothamBold; label.TextSize = 12; label.TextXAlignment = Enum.TextXAlignment.Left
    
    local bg = Instance.new("Frame", container)
    bg.Size = UDim2.new(1, 0, 0, 10); bg.Position = UDim2.new(0, 0, 0, 20)
    bg.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    Instance.new("UICorner", bg).CornerRadius = UDim.new(1,0)
    local stroke = Instance.new("UIStroke", bg); stroke.Color = Color3.fromRGB(60, 60, 70)
    
    local fill = Instance.new("Frame", bg)
    fill.Size = UDim2.new((default-min)/(max-min), 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1,0)
    
    local isDragging = false
    bg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isDragging = true end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then isDragging = false end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local mousePos = UserInputService:GetMouseLocation().X
            local bgPos = bg.AbsolutePosition.X; local bgSize = bg.AbsoluteSize.X
            local pct = math.clamp((mousePos - bgPos) / bgSize, 0, 1)
            fill.Size = UDim2.new(pct, 0, 1, 0)
            local val = math.floor(min + (max - min) * pct)
            label.Text = text .. ": " .. val
            callback(val)
        end
    end)
end

local function updateToggleBtn(btn, state, prefix)
    btn.Text = prefix .. (state and "AÇIK" or "KAPALI")
    local targetColor = state and Color3.fromRGB(0, 170, 100) or Color3.fromRGB(200, 40, 40)
    TweenService:Create(btn, TweenInfo.new(0.3), {BackgroundColor3 = targetColor}):Play()
end

---------------------------------------------------------
-- FLING SİSTEMİ
---------------------------------------------------------
local flingContainer = Instance.new("Frame", tbPage)
flingContainer.Size = UDim2.new(0.9, 0, 0, 110)
flingContainer.BackgroundTransparency = 1
Instance.new("UIListLayout", flingContainer).SortOrder = Enum.SortOrder.LayoutOrder

local flingLabel = Instance.new("TextLabel", flingContainer)
flingLabel.Size = UDim2.new(1, 0, 0, 20)
flingLabel.BackgroundTransparency = 1
flingLabel.Text = "Fling (Uçurma) Ayarları"
flingLabel.TextColor3 = Color3.fromRGB(0, 170, 255)
flingLabel.Font = Enum.Font.GothamBold
flingLabel.TextSize = 12
flingLabel.TextXAlignment = Enum.TextXAlignment.Left

local flingTopMenu = Instance.new("Frame", flingContainer)
flingTopMenu.Size = UDim2.new(1, 0, 0, 35)
flingTopMenu.BackgroundTransparency = 1

local yonelmeEnabled = false 
local yonelmeBtn = createBtn("Yönelme (E): KAPALI", tbPage)

aimAssistBtn.MouseButton1Click:Connect(function()
    aimAssistEnabled = not aimAssistEnabled
    updateToggleBtn(aimAssistBtn, aimAssistEnabled, "AimAssist: ")
    SendNotification("AimAssist", aimAssistEnabled and "Aktif edildi." or "Kapatıldı.", 2)
end)
yonelmeBtn.MouseButton1Click:Connect(function()
    yonelmeEnabled = not yonelmeEnabled
    yonelmeBtn.Text = "Yönelme (E): " .. (yonelmeEnabled and "AÇIK" or "KAPALI")
    local targetColor = yonelmeEnabled and Color3.fromRGB(0, 170, 100) or Color3.fromRGB(35, 35, 40)
    TweenService:Create(yonelmeBtn, TweenInfo.new(0.3), {BackgroundColor3 = targetColor}):Play()
    SendNotification("Yönelme", yonelmeEnabled and "Aktif Edildi (E)" or "Kapatıldı", 2)
end)

local targetBtn = createBtn("Hedef: Yok ▼", flingTopMenu, Color3.fromRGB(35, 35, 40))
targetBtn.Size = UDim2.new(0.65, 0, 1, 0)
targetBtn.Position = UDim2.new(0, 0, 0, 0)



local refreshBtn = createBtn("Yenile", flingTopMenu, Color3.fromRGB(0, 120, 200))
refreshBtn.Size = UDim2.new(0.32, 0, 1, 0)
refreshBtn.Position = UDim2.new(0.68, 0, 0, 0)

local padding = Instance.new("Frame", flingContainer)
padding.Size = UDim2.new(1, 0, 0, 5); padding.BackgroundTransparency = 1

local flingActionBtn = createBtn("Fling BAŞLAT", flingContainer, Color3.fromRGB(200, 40, 40))

local targetListFrame = Instance.new("ScrollingFrame", tbPage)
targetListFrame.Size = UDim2.new(0.9, 0, 0, 120)
targetListFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
targetListFrame.Visible = false
targetListFrame.ScrollBarThickness = 4
targetListFrame.ScrollBarImageColor3 = Color3.fromRGB(0, 170, 255)
targetListFrame.BorderSizePixel = 0
Instance.new("UICorner", targetListFrame).CornerRadius = UDim.new(0, 6)
local listLayout = Instance.new("UIListLayout", targetListFrame)
listLayout.Padding = UDim.new(0, 4)

local selectedFlingTarget = nil
local flingActive = false
local flingConn = nil

local function refreshPlayerList()
    for _, child in pairs(targetListFrame:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end
    local players = Players:GetPlayers()
    targetListFrame.CanvasSize = UDim2.new(0, 0, 0, #players * 30)
    for _, p in pairs(players) do
        if p ~= localPlayer then
            local pBtn = Instance.new("TextButton", targetListFrame)
            pBtn.Size = UDim2.new(1, -8, 0, 26)
            pBtn.Position = UDim2.new(0, 4, 0, 0)
            pBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
            -- Görünen adı (DisplayName) kullanıyoruz
            pBtn.Text = p.DisplayName; pBtn.TextColor3 = Color3.new(1,1,1)
            pBtn.Font = Enum.Font.GothamSemibold; pBtn.TextSize = 12
            Instance.new("UICorner", pBtn).CornerRadius = UDim.new(0, 4)
            pBtn.MouseButton1Click:Connect(function()
                PlaySound("Click")
                selectedFlingTarget = p
                targetBtn.Text = "Hedef: " .. p.DisplayName .. " ▼"
                targetListFrame.Visible = false
                SendNotification("Hedef Seçildi", "Fling Hedefi: " .. p.DisplayName, 2)
            end)
        end
    end
end

refreshBtn.MouseButton1Click:Connect(function()
    refreshPlayerList()
    SendNotification("Liste Yenilendi", "Oyuncu listesi güncellendi.", 2)
end)

targetBtn.MouseButton1Click:Connect(function()
    targetListFrame.Visible = not targetListFrame.Visible
    if targetListFrame.Visible then refreshPlayerList() end
end)

local originalPos = nil 
local startTime = 0

flingActionBtn.MouseButton1Click:Connect(function()
    if not selectedFlingTarget then 
        SendNotification("Hata", "Önce bir hedef seçmelisiniz!", 3)
        return 
    end
    
    flingActive = not flingActive
    
    if flingActive then
        updateToggleBtn(flingActionBtn, true, "Fling DURDUR")
        
        local myChar = localPlayer.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        
        if myHrp then
            originalPos = myHrp.CFrame
            startTime = tick()
            SendNotification("Fling", selectedFlingTarget.DisplayName .. " takip ediliyor...", 2)
            
            flingConn = RunService.Heartbeat:Connect(function()
                local tChar = selectedFlingTarget.Character
                local tHrp = tChar and tChar:FindFirstChild("HumanoidRootPart")
                
                if myChar and tChar and myHrp and tHrp then
                    local elapsed = tick() - startTime
                    
                    -- Çarpışma (CanCollide) ayarları silindi, dokunulmuyor
                    
                    -- Hareket Tahmini ve Alan Tarama
                    local predictionOffset = tHrp.AssemblyLinearVelocity * 0.12
                    local angle = tick() * 25
                    local radius = 2.5
                    local areaOffset = Vector3.new(math.sin(angle) * radius, 0, math.cos(angle) * radius)

                    if elapsed < 1 then
                        -- Sadece takip
                        myHrp.CFrame = tHrp.CFrame + predictionOffset
                        myHrp.AssemblyAngularVelocity = Vector3.zero
                    else
                        -- 1 saniye sonra uçurma başlar
                        myHrp.CFrame = tHrp.CFrame + predictionOffset + areaOffset
                        myHrp.AssemblyAngularVelocity = Vector3.new(99999, 99999, 99999) 
                        myHrp.AssemblyLinearVelocity = Vector3.new(500, 500, 500)
                    end
                else
                    flingActive = false
                    flingActionBtn.MouseButton1Click:Fire() 
                end
            end)
        end
    else
        -- DURDURMA MANTIĞI
        updateToggleBtn(flingActionBtn, false, "Fling BAŞLAT")
        if flingConn then flingConn:Disconnect() end
        
        local myChar = localPlayer.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        
        if myHrp then
            myHrp.AssemblyAngularVelocity = Vector3.zero
            myHrp.AssemblyLinearVelocity = Vector3.zero
            
            -- KOŞUL: Sadece 1 saniyeden fazla sürdüyse geri ışınla
            local totalElapsed = tick() - startTime
            if originalPos and totalElapsed >= 1 then
                myHrp.CFrame = originalPos
                SendNotification("Fling Bitti", "Başlangıç noktasına dönüldü.", 2)
            else
                SendNotification("Fling İptal", "Erken kapatıldı, yerinde kaldın.", 2)
            end
        end
    end
end)

local lineDiv = Instance.new("Frame", tbPage)
lineDiv.Size = UDim2.new(0.9, 0, 0, 2)
lineDiv.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
lineDiv.BorderSizePixel = 0

local pEspBtn = createBtn("Oyuncu ESP: KAPALI", tbPage)
local Triggerbot = createBtn("Triggerbot", RivalsPage)


local flyMenuBtn = createBtn("Uçma (Y): KAPALI", tbPage)
local rGrabBtn = createBtn("R Yakalama: KAPALI", tbPage)
local ragdollBtn = createBtn("Ragdoll: KAPALI", tbPage)
local autoBlockBtn = createBtn("AutoBlock: KAPALI", tbPage)

local speedToggleBtn = createBtn("Hız Sabitleyici: KAPALI", tbPage, Color3.fromRGB(200, 40, 40))
createSlider("Karakter Hızı", tbPage, 16, 400, 16, function(val) selectedSpeed = val end)

local jumpToggleBtn = createBtn("Zıplama Sabitleyici: KAPALI", tbPage, Color3.fromRGB(200, 40, 40))
createSlider("Zıplama Gücü", tbPage, 50, 300, 50, function(val) selectedJump = val end)

local speed100Btn = createBtn("Hızı 100 Yap (Tek Seferlik)", tbPage, Color3.fromRGB(0, 120, 200), Color3.fromRGB(0, 200, 255))
local escapeBtn = createBtn("det kontur yememe", tbPage, Color3.fromRGB(150, 40, 200))

local mEspBtn = createBtn("Canavar ESP: KAPALI", doorsPage)
local noclipBtn = createBtn("Noclip: KAPALI", doorsPage)
local a90Btn = createBtn("A90 Remote Tetikle", doorsPage)

---------------------------------------------------------
-- TÜM ÖZELLİK MANTIKLARI & BUTON ETKİLEŞİMLERİ
---------------------------------------------------------
tbTab.MouseButton1Click:Connect(function()
    tbPage.Visible = true
    doorsPage.Visible = false
    RivalsPage.Visible = false

    tbTab.TextColor3 = Color3.fromRGB(0,170,255)
    doorsTab.TextColor3 = Color3.fromRGB(200,200,200)
    Rivalstab.TextColor3 = Color3.fromRGB(200,200,200)
end)

doorsTab.MouseButton1Click:Connect(function()
    tbPage.Visible = false
    doorsPage.Visible = true
    RivalsPage.Visible = false

    tbTab.TextColor3 = Color3.fromRGB(200,200,200)
    doorsTab.TextColor3 = Color3.fromRGB(0,170,255)
    Rivalstab.TextColor3 = Color3.fromRGB(200,200,200)
end)

Rivalstab.MouseButton1Click:Connect(function()
    tbPage.Visible = false
    doorsPage.Visible = false
    RivalsPage.Visible = true

    tbTab.TextColor3 = Color3.fromRGB(200,200,200)
    doorsTab.TextColor3 = Color3.fromRGB(200,200,200)
    Rivalstab.TextColor3 = Color3.fromRGB(0,170,255)
end)

pEspBtn.MouseButton1Click:Connect(function() 
    espEnabled = not espEnabled; 
    pEspBtn.Text = "Oyuncu ESP: "..(espEnabled and "AÇIK" or "KAPALI") 
    SendNotification("Oyuncu ESP", espEnabled and "Aktif edildi." or "Kapatıldı.", 2)
end)

mEspBtn.MouseButton1Click:Connect(function() 
    monsterEspEnabled = not monsterEspEnabled; 
    mEspBtn.Text = "Canavar ESP: "..(monsterEspEnabled and "AÇIK" or "KAPALI")
    SendNotification("Canavar ESP", monsterEspEnabled and "Aktif edildi." or "Kapatıldı.", 2)
end)

flyMenuBtn.MouseButton1Click:Connect(function() 
    flyEnabled = not flyEnabled; 
    flyMenuBtn.Text = "Uçma (Y): "..(flyEnabled and "HAZIR" or "KAPALI")
    SendNotification("Uçuş Modu", flyEnabled and "Y Tuşu ile uçabilirsiniz." or "Kapatıldı.", 2)
end)



rGrabBtn.MouseButton1Click:Connect(function() 
    rKeyEnabled = not rKeyEnabled; 
    rGrabBtn.Text = "R Yakalama: "..(rKeyEnabled and "AÇIK" or "KAPALI")
    SendNotification("R Yakalama", rKeyEnabled and "R Tuşu ile yakalama aktif." or "Kapatıldı.", 2)
end)

ragdollBtn.MouseButton1Click:Connect(function() 
    ragdollEnabled = not ragdollEnabled; 
    ragdollBtn.Text = "Ragdoll: "..(ragdollEnabled and "AÇIK" or "KAPALI")
    SendNotification("Ragdoll", ragdollEnabled and "Aktif edildi." or "Kapatıldı.", 2)
end)



Triggerbot.MouseButton1Click:Connect(function()
    isEnabled = not isEnabled
    
    if isEnabled then
        print("Gelişmiş Kafa Triggerbot (Saç/Şapka Korumalı): AÇIK")
        
        local lastClick = 0
        local RunService = game:GetService("RunService")
        local Players = game:GetService("Players")
        local VirtualInputManager = game:GetService("VirtualInputManager")
        
        connection = RunService.RenderStepped:Connect(function()
            local localPlayer = Players.LocalPlayer
            local camera = workspace.CurrentCamera
            local character = localPlayer.Character
            
            if not character or not character:FindFirstChild("Humanoid") then return end
            
            local params = RaycastParams.new()
            params.FilterType = Enum.RaycastFilterType.Exclude
            params.FilterDescendantsInstances = {character}
            
            local rayOrigin = camera.CFrame.Position
            local rayDirection = camera.CFrame.LookVector * 1000
            
            local result = workspace:Raycast(rayOrigin, rayDirection, params)
            
            if result and result.Instance then
                local targetModel = result.Instance:FindFirstAncestorOfClass("Model")
                
                if targetModel then
                    local targetPlayer = Players:GetPlayerFromCharacter(targetModel)
                    local targetHumanoid = targetModel:FindFirstChildOfClass("Humanoid")
                    local targetHead = targetModel:FindFirstChild("Head")
                    
                    -- Hedef geçerli mi, yaşıyor mu ve bir "Kafası" var mı?
                    if targetPlayer and targetPlayer ~= localPlayer and targetHumanoid and targetHumanoid.Health > 0 and targetHead then
                        local isTeammate = (localPlayer.Team ~= nil and localPlayer.Team == targetPlayer.Team)
                        
                        if not isTeammate then
                            -- KRİTİK NOKTA: Işının çarptığı yer ile hedefin kafası arasındaki mesafe
                            local distanceToHead = (result.Position - targetHead.Position).Magnitude
                            
                            -- Eğer çarpma noktası kafaya 1.5 stud veya daha yakınsa (saç, kask, gözlük dahil)
                            if distanceToHead <= 1.5 then
                                local now = os.clock()
                                if now - lastClick >= 0.001 then
                                    lastClick = now
                                    local vp = camera.ViewportSize
                                    
                                    -- Anında sol tık gönder
                                    VirtualInputManager:SendMouseButtonEvent(vp.X / 2, vp.Y / 2, 0, true, game, 0)
                                    task.defer(function()
                                        VirtualInputManager:SendMouseButtonEvent(vp.X / 2, vp.Y / 2, 0, false, game, 0)
                                    end)
                                end
                            end
                        end
                    end
                end
            end
        end)
    else
        print("Kafa Triggerbot: KAPALI")
        if connection then
            connection:Disconnect()
            connection = nil
        end
    end
end)


noclipBtn.MouseButton1Click:Connect(function() 
    noclipEnabled = not noclipEnabled; 
    noclipBtn.Text = "Noclip: "..(noclipEnabled and "AÇIK" or "KAPALI")
    SendNotification("Noclip", noclipEnabled and "Duvarlardan geçme aktif." or "Kapatıldı.", 2)
end)

speedToggleBtn.MouseButton1Click:Connect(function() 
    speedLoopEnabled = not speedLoopEnabled
    updateToggleBtn(speedToggleBtn, speedLoopEnabled, "Hız Sabitleyici: ")
    SendNotification("Hız Sabitleyici", speedLoopEnabled and "Açık" or "Kapalı", 2)
end)

jumpToggleBtn.MouseButton1Click:Connect(function() 
    jumpLoopEnabled = not jumpLoopEnabled
    updateToggleBtn(jumpToggleBtn, jumpLoopEnabled, "Zıplama Sabitleyici: ")
    SendNotification("Zıplama Sabitleyici", jumpLoopEnabled and "Açık" or "Kapalı", 2)
end)

-- AutoBlock Buton Bağlantısı
autoBlockBtn.MouseButton1Click:Connect(function()
    autoBlockEnabled = not autoBlockEnabled
    updateToggleBtn(autoBlockBtn, autoBlockEnabled, "AutoBlock: ")
    SendNotification("AutoBlock", autoBlockEnabled and "Aktif edildi." or "Kapatıldı.", 2)
end)

-- VirtualInputManager servisini "F" tuşuna basma simülasyonu için ekliyoruz

local VirtualInputManager = game:GetService("VirtualInputManager")
local isHoldingF = false

RunService.Heartbeat:Connect(function()
    -- Özellik kapalıysa F'i bırak ve durdur
    if not autoBlockEnabled then 
        if isHoldingF then
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.F, false, game)
            isHoldingF = false
        end
        return 
    end
    if aimAssistEnabled then
    -- 1. FOV Halkasını farenin etrafında göster ve takip ettir
    local mousePos = UserInputService:GetMouseLocation()
    fovCircle.Position = mousePos
    fovCircle.Radius = fovRadius
    fovCircle.Visible = true

    -- 2. CTRL ile Kilitlenme Aktifse Halkanın İçini Tara
    if isAimLocked then
        -- Eğer mevcut hedef ölürse veya kaybolursa yeni hedef ara
        if not aimTarget or not aimTarget.Parent or not aimTarget.Parent:FindFirstChildOfClass("Humanoid") or aimTarget.Parent:FindFirstChildOfClass("Humanoid").Health <= 0 then
            aimTarget = nil
            local closestDist = fovRadius
            
            -- Oyuncuları / Canlıları tara
            local liveFolder = workspace:FindFirstChild("Live")
            local targetsList = liveFolder and liveFolder:GetChildren() or Players:GetPlayers()
            
            for _, obj in pairs(targetsList) do
                local char = (obj:IsA("Player") and obj.Character) or obj
                if char and char ~= localPlayer.Character then
                    local hum = char:FindFirstChildOfClass("Humanoid")
                    local head = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart")
                    
                    if hum and hum.Health > 0 and head then
                        local screenPos, onScreen = camera:WorldToViewportPoint(head.Position)
                        if onScreen then
                            -- Farenin ekran konumuna olan mesafeyi ölç
                            local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                            
                            -- Sadece halkanın içindeyse ve en yakınsa hedef al
                            if dist <= closestDist then
                                closestDist = dist
                                aimTarget = head
                            end
                        end
                    end
                end
            end
        end

        -- 3. Halka içinde bulunan hedefe kamerayı kilitle
        if aimTarget then
            camera.CFrame = CFrame.lookAt(camera.CFrame.Position, aimTarget.Position)
        end
    end
else
    -- AimAssist kapandığında her şeyi sıfırla
    fovCircle.Visible = false
    isAimLocked = false
    aimTarget = nil
end

    local myChar = localPlayer.Character
    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHrp then return end

    -- En yakın hedefi bul
    local targetHrp = getNearestTarget()
    
    if targetHrp then
        local targetChar = targetHrp.Parent
        local distance = (targetHrp.Position - myHrp.Position).Magnitude

        -- 10 stud yakınlık kontrolü
        if distance <= 15 then
            -- Aksesuar kontrolleri
            local hasHunterFists = targetChar:FindFirstChild("HunterFists") and targetChar.HunterFists:IsA("Accessory")
            local hasM1ing = targetChar:FindFirstChild("M1ing") and targetChar.M1ing:IsA("Accessory")

            -- Eğer herhangi biri varsa (veya ikisi birden)
            if hasHunterFists or hasM1ing then
                -- Karakteri hedefe doğru döndür (Kilitleme)
                local targetPos = Vector3.new(targetHrp.Position.X, myHrp.Position.Y, targetHrp.Position.Z)
                myHrp.CFrame = CFrame.lookAt(myHrp.Position, targetPos)

                -- F tuşuna basılı tut
                if not isHoldingF then
                    VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.F, false, game)
                    isHoldingF = true
                end
            else
                -- Aksesuarlar yoksa F'i bırak
                if isHoldingF then
                    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.F, false, game)
                    isHoldingF = false
                end
            end
        else
            -- 10 stud dışına çıktıysa F'i bırak
            if isHoldingF then
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.F, false, game)
                isHoldingF = false
            end
        end
    else
        -- Yakında kimse yoksa F'i bırak
        if isHoldingF then
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.F, false, game)
            isHoldingF = false
        end
    end
end)




speed100Btn.MouseButton1Click:Connect(function()
    local char = localPlayer.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = 100 end
        SendNotification("Hız Artırıldı", "Karakter hızı tek seferlik 100 yapıldı.", 2)
    end
end)

-- KAÇMA ÖZELLİĞİ (SADECE 3 SANİYE)
local isEscaping = false

local function startEscape()
    if isEscaping then return end
    
    local char = localPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    
    if char and hrp and hum then
        isEscaping = true
        SendNotification("Kaçış Başlatıldı", "3 saniye boyunca havaya sabitleniyorsunuz...", 3)
        local originalCFrame = hrp.CFrame
        local destroyHeight = game.Workspace.FallenPartsDestroyHeight
        
        local escapePos = Vector3.new(0, destroyHeight + 2.5, 0)
        local escapeRot = CFrame.Angles(math.rad(90), 0, 0)
        
        hum.PlatformStand = true
        hum.AutoRotate = false
        
        local bp = Instance.new("BodyPosition", hrp)
        bp.MaxForce = Vector3.new(1, 1, 1) * math.huge
        bp.Position = escapePos
        bp.P = 50000 
        
        local bg = Instance.new("BodyGyro", hrp)
        bg.MaxTorque = Vector3.new(1, 1, 1) * math.huge
        bg.CFrame = escapeRot
        bg.P = 500000 
        bg.D = 500    
        
        hrp.CFrame = CFrame.new(escapePos) * escapeRot
        escapeBtn.Text = "Kaçılıyor (3s)..."
        
        task.wait(3)
        
        if char and hrp and hum then
            bp:Destroy()
            bg:Destroy()
            hum.PlatformStand = false
            hum.AutoRotate = true
            hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
            hrp.AssemblyAngularVelocity = Vector3.new(0,0,0)
            hrp.CFrame = originalCFrame
            task.wait(0.1)
            workspace.CurrentCamera.CameraType = Enum.CameraType.Custom
            workspace.CurrentCamera.CameraSubject = hum
        end
        
        escapeBtn.Text = "det kontur yememe"
        isEscaping = false
        SendNotification("Kaçış Bitti", "Eski konumunuza döndünüz.", 2)
    end
end

escapeBtn.MouseButton1Click:Connect(startEscape)

---------------------------------------------------------
-- DÖNGÜ VE INPUT
---------------------------------------------------------
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    
    local flySpeed = 350
    local flyConn
    local customFlyVel 
    local customFlyOri

    if input.KeyCode == Enum.KeyCode.V then
        startEscape()
    end

    if input.KeyCode == Enum.KeyCode.Y and flyEnabled then
        isFlying = not isFlying
        local hrp = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
        local humanoid = localPlayer.Character and localPlayer.Character:FindFirstChild("Humanoid")
        
        if hrp and humanoid then
            if isFlying then
                if hrp:FindFirstChild("CustomFlyVelocity") then hrp.CustomFlyVelocity:Destroy() end
                if hrp:FindFirstChild("CustomFlyGyro") then hrp.CustomFlyGyro:Destroy() end
                if hrp:FindFirstChild("CustomFlyAttachment") then hrp.CustomFlyAttachment:Destroy() end

                local att = Instance.new("Attachment", hrp)
                att.Name = "CustomFlyAttachment"

                customFlyVel = Instance.new("LinearVelocity")
                customFlyVel.Name = "CustomFlyVelocity" 
                customFlyVel.MaxForce = 10^10 
                customFlyVel.VelocityConstraintMode = Enum.VelocityConstraintMode.Vector
                customFlyVel.Attachment0 = att
                customFlyVel.Parent = hrp

                customFlyOri = Instance.new("AlignOrientation")
                customFlyOri.Name = "CustomFlyGyro" 
                customFlyOri.MaxTorque = 10^10
                customFlyOri.Responsiveness = 200
                customFlyOri.Mode = Enum.OrientationAlignmentMode.OneAttachment
                customFlyOri.Attachment0 = att
                customFlyOri.Parent = hrp

                humanoid.PlatformStand = true

                flyConn = RunService.RenderStepped:Connect(function()
                    if not isFlying or not hrp.Parent then
                        if flyConn then flyConn:Disconnect() end
                        return
                    end

                    local camCF = workspace.CurrentCamera.CFrame
                    customFlyOri.CFrame = camCF 
                    
                    local moveDir = Vector3.new(0, 0, 0)
                    
                    if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camCF.LookVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camCF.LookVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camCF.RightVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camCF.RightVector end
                    if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
                    if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end

                    if moveDir.Magnitude > 0 then
                        customFlyVel.VectorVelocity = moveDir.Unit * flySpeed
                    else
                        customFlyVel.VectorVelocity = Vector3.new(0, 0, 0)
                    end
                end)
            else
                if hrp:FindFirstChild("CustomFlyVelocity") then hrp.CustomFlyVelocity:Destroy() end
                if hrp:FindFirstChild("CustomFlyGyro") then hrp.CustomFlyGyro:Destroy() end
                if hrp:FindFirstChild("CustomFlyAttachment") then hrp.CustomFlyAttachment:Destroy() end
                if flyConn then flyConn:Disconnect() end
                
                humanoid.PlatformStand = false
                humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
        end
    end

    if input.KeyCode == Enum.KeyCode.R and rKeyEnabled and not isAttached then
        local targetHRP = getNearestTarget()
        if targetHRP then
            isAttached = true
            local character = localPlayer.Character
            local myHRP = character.HumanoidRootPart
            local myHumanoid = character:FindFirstChildOfClass("Humanoid")
            local startTime = tick()
            local conn
            
            conn = RunService.Heartbeat:Connect(function()
                if tick() - startTime > 0.5 or not targetHRP.Parent or not myHRP.Parent then
                    isAttached = false
                    myHRP.AssemblyLinearVelocity = Vector3.zero 
                    myHRP.AssemblyAngularVelocity = Vector3.zero 
                    if myHumanoid then
                        myHumanoid:ChangeState(Enum.HumanoidStateType.Landed)
                    end
                    conn:Disconnect() 
                    return
                end
                myHRP.CFrame = CFrame.new(targetHRP.Position + Vector3.new(0, 3, 0)) * CFrame.Angles(math.rad(90), 0, 0)
                myHRP.AssemblyLinearVelocity = Vector3.new(0, 200, 0)
            end)
        end
    end

    if input.KeyCode == Enum.KeyCode.E and yonelmeEnabled then
        isYonelmeActive = not isYonelmeActive

        local myChar = localPlayer.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        local humanoid = myChar and myChar:FindFirstChild("Humanoid")

        if not isYonelmeActive then
            if yonelmeConn then yonelmeConn:Disconnect() end
            if currentAlignOri then currentAlignOri:Destroy() end
            if currentAtt then currentAtt:Destroy() end
            if humanoid then humanoid.AutoRotate = true end
            if myHrp then
                myHrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                myHrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
            end
            return 
        end

        local targetHRP = getNearestTarget()
        if targetHRP and myHrp and humanoid then
            if yonelmeConn then yonelmeConn:Disconnect() end
            
            currentAtt = Instance.new("Attachment", myHrp)
            currentAlignOri = Instance.new("AlignOrientation")
            currentAlignOri.Mode = Enum.OrientationAlignmentMode.OneAttachment
            currentAlignOri.Attachment0 = currentAtt
            currentAlignOri.MaxTorque = 10^7
            currentAlignOri.Responsiveness = 200
            currentAlignOri.CFrame = CFrame.Angles(math.rad(90), 0, 0)
            currentAlignOri.Parent = myHrp

            humanoid.AutoRotate = false
            
            yonelmeConn = RunService.Heartbeat:Connect(function()
                if not targetHRP.Parent or not myHrp.Parent then
                    isYonelmeActive = false
                    if yonelmeConn then yonelmeConn:Disconnect() end
                    if currentAlignOri then currentAlignOri:Destroy() end
                    if currentAtt then currentAtt:Destroy() end
                    humanoid.AutoRotate = true
                    return
                end
                
                local currentY = myHrp.Position.Y 
                local targetPos = targetHRP.Position
                myHrp.CFrame = CFrame.new(targetPos.X, currentY, targetPos.Z) * CFrame.Angles(math.rad(90), 0, 0)
            end)
        else
            isYonelmeActive = false
        end
    end
end)


if (input.KeyCode == Enum.KeyCode.LeftControl or input.KeyCode == Enum.KeyCode.RightControl) and aimAssistEnabled then
    isAimLocked = not isAimLocked
    if isAimLocked then
        SendNotification("AimAssist", "Halka İçi Hedefe Kilitlendi!", 1.5)
    else
        SendNotification("AimAssist", "Kilit Serbest Bırakıldı.", 1.5)
        aimTarget = nil
    end
end

RunService.RenderStepped:Connect(function()
    local char = localPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    
    if hum then
        if speedLoopEnabled then hum.WalkSpeed = selectedSpeed end
        if jumpLoopEnabled then hum.UseJumpPower = true; hum.JumpPower = selectedJump end
    end

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= localPlayer and p.Character then
            local h = p.Character:FindFirstChild("AydoESP") or Instance.new("Highlight", p.Character)
            h.Name = "AydoESP"; h.Enabled = espEnabled
        end
    end
    
    if monsterEspEnabled then
        for _, obj in pairs(workspace:GetChildren()) do
            if obj:IsA("Model") and obj:FindFirstChildOfClass("Humanoid") and not Players:GetPlayerFromCharacter(obj) then
                local h = obj:FindFirstChild("MonsterESP") or Instance.new("Highlight", obj)
                h.Name = "MonsterESP"; h.Enabled = true; h.FillColor = Color3.new(1, 0.5, 0)
            end
        end
    end

    if isFlying and flyBodyVelocity and flyBodyGyro then
        local dir = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + camera.CFrame.RightVector end
        flyBodyVelocity.Velocity = dir.Magnitude > 0 and dir.Unit * flySpeed or Vector3.zero
        flyBodyGyro.CFrame = camera.CFrame
    end

    if noclipEnabled and char then
        for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end
    end
    
    if ragdollEnabled and hum then 
        for _, t in pairs(hum:GetPlayingAnimationTracks()) do t:Stop() end
    end
end)
